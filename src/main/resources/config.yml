# URL of resource pack - default is the GitHub link
# Leave blank to use no pack
pack-url: "https://raw.githubusercontent.com/cometcake575/InfectionIsland/main/Resource%20Pack.zip"
# Enforce using the resource pack
enforce-pack: true
# Resource pack message
pack-message: "Infection resource pack"

# Number of lives each player currently has
lives: {}
